/**
 * Theme hooks — convenience re-exports from ThemeContext.
 *
 * Import `useTheme` for the full context or `useColors` for just the
 * resolved colour tokens.
 */

export { useTheme, useColors } from '@/contexts/ThemeContext';
